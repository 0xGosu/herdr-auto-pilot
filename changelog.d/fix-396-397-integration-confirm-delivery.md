- Fixed the two confirm-delivery integration tests, which could not pass since 0.8.0 and had silently disabled the guard on the send-content regression (that confirming a label reply selects the numbered menu digit rather than pasting the label). They now run their own daemon over their own store, because an operator's confirm is queued for the owning node's daemon to type rather than sent inline. `TestRealClaudeConsult` carried the same breakage behind `HAP_ITEST_CLAUDE=1` and is fixed with them.
- Fixed the two task-summary integration tests, which escalated `unclassifiable`/`over_masked` instead of consulting: their scratch pane's output was short enough to fit on screen, and the daemon's classification read (`pane read --source recent`) is a scrollback delta that returns nothing until a pane has scrolled. The fixtures now scroll, and they wait for the pane's own content instead of sleeping a fixed second.
